---
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: scarfsage-bucket2-rewrite
  namespace: storage
spec:
  replacePathRegex:
    regex: "^/(.*)"
    replacement: "/scarfsage/$1"
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: scarfsage-bucket2-ingress
  namespace: storage
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: storage-scarfsage-bucket2-rewrite@kubernetescrd
spec:
  ingressClassName: traefik
  rules:
    - host: &host "b.scarfsage.com"
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: minio
                port:
                  name: minio-api
  tls:
    - hosts:
        - *host
      secretName: *host
